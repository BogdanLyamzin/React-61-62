import styled from "@emotion/styled";

const Wrapper = styled.main`
    padding: 20px 0;
`

const Main = () => {
    return (
        <Wrapper>
            <h2>Page title</h2>
        </Wrapper>
    )
}

export default Main;